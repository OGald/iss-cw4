#!/bin/bash
# Sudo run this script just to chmod all of the other scripts so they can work
chmod +x solomon-build.sh
chmod +x solomon-remove.sh
chmod +x solomon-reset.sh
chmod +x solomon-restart.sh
chmod +x solomon-run.sh
chmod +x solomon-sec-config.sh
